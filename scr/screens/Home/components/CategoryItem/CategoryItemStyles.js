import { StyleSheet } from "react-native"

export default styles = StyleSheet.create({
    cardContainer:{
        marginHorizontal: 30,
        marginVertical:10,
        padding:10,
        justifyContent: 'center',
        alignContent: 'center',
    },
    text:{
        fontSize: 25,
        fontFamily: 'Kalam',
    }
})